Use the `game-designer` agent to design or review the following: $ARGUMENTS

Process:
1. Analyze request with game design principles
2. Produce a design document in Markdown format
3. Save to `docs/design/[feature-name].md`
4. Run `codex:rescue` to review technical feasibility of the design

Output must include: core mechanics, player motivation, success metrics, and open questions.
